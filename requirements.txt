# 茶叶冲泡定时提醒程序依赖库
# Tea Brewing Timer Application Dependencies

# 图像处理库 - 用于背景图片和UI图片处理
Pillow>=9.0.0

# 数据可视化库 - 用于趋势分析图表
matplotlib>=3.5.0

# 数值计算库 - 用于数据分析
numpy>=1.21.0

# GUI增强库（可选）
# tkinter 是Python标准库，无需单独安装

# 其他可能需要的库
# 如果在某些系统上遇到字体问题，可以取消注释以下库：
# fonttools>=4.0.0

# 如果需要更好的图像格式支持，可以取消注释：
# pillow-heif>=0.10.0

# 安装说明：
# 1. 确保已安装Python 3.7或更高版本
# 2. 在命令行中运行：pip install -r requirements.txt
# 3. 或者双击run.bat文件自动安装并启动程序